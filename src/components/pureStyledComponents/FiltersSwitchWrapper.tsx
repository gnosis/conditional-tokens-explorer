import styled from 'styled-components'

export const FiltersSwitchWrapper = styled.div`
  display: block;
  position: relative;

  .filterResultsText {
    bottom: -23px;
    left: 2px;
    position: absolute;
  }
`
